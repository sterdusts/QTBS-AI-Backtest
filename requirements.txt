gradio>=4.0.0
openai>=1.0.0
python-dotenv>=1.0.0

# pandas>=3.0 required: the data layer / engines rely on Copy-on-Write
# (always on since 3.0) for zero-copy anti-tampering; 2.x defaults CoW off.
# NOTE: keep this file ASCII-only -- pip decodes it with the system locale
# (cp950 on zh-TW Windows) when there is no BOM, and non-ASCII bytes crash it.
pandas>=3.0.0
numpy>=1.24.0

pyecharts>=2.0.0
requests>=2.31.0
python-dateutil>=2.8.2

python-binance>=1.0.19

# dev / test
pytest>=8.0.0
